import { commandRanks } from "./command"
import { armyRanks } from "./army"
import { starfighterRanks } from "./starfighter"
import { specialRanks } from "./special"
import { navalRanks } from "./naval"

export const ranks = {
  command: commandRanks,
  army: armyRanks,
  starfighter: starfighterRanks,
  special: specialRanks,
  naval: navalRanks,
}
